### GOALS FOR MY PROJECT
1. Reduce LLM token costs by 70-90% using semantic similarity caching;
2. Implement DIFFERENT token-control strategies, namely reject, truncate with warning, and summarise overflow;
3. Use modern tools: FastAPI, Redis, Chroma, transformers, LangChain, Docker and deeply understand their roles;
4. Implement consistent mock testing throughout all functions of the project.

========================================================================================================================

### SEMANTIC CACHING:
The traditional caching works as follows:
• storing exact query → response.
    If user asks: "What is your return policy?" → cached.
    If user asks: "What's your return policy?" (different wording) → cache miss → LLM call → tokens burned.
The Semantic caching works as follows:
• storing embedding of query → response.
    Both queries have similar embeddings (example above) → cache hit → instant response → zero tokens used.

### SEMANTIC CACHING WORKFLOW:
1. Client POST /chat with { "query": "How do I return a defective product?", "context": "optional prior history" }
2. FastAPI receives, creates a unique cache key = (context + query)
3. Compute embedding of key using same embedder (all-MiniLM-L6-v2)
4. Search Chroma for similar embeddings (cosine similarity)
   - If similarity >= threshold (e.g., 0.92) → cache hit
     • Return stored response immediately (latency ~10ms)
     • Log cache hit, tokens saved = length_of_stored_response
   - Else → cache miss
     • Apply token control: check if prompt exceeds max_prompt_tokens. If yes, apply their token control strategy chosen
        (default is reject); otherwise proceed.
   - Call LLM (async, with retries)
     • Store in:
       a) Redis (exact key → response, TTL) for exact-match speed
       b) Chroma (embedding → response metadata) for semantic search
     • Return LLM response
5. Response includes metadata: cache_hit, tokens_used, tokens_saved, latency_ms

------------------------------------------------------------------------------------------------------------------------

### TOKEN CONTROL WORKFLOW:
• Use transformers to accurately count prompt tokens.
• We implement three configurable strategies (overflow_strategy): token_count > max_prompt_tokens, then either
   1. we reject the request with error HTTP 400 and a clear message:
       "Prompt too long (X tokens, limit Y). Please shorten your input.";
   2. we truncate with a warning and details of the percentage truncated at the beginning and the end and return a
       detailed warning;
   3. we keep 70% of the prompt and summarize the rest, to fill out the remaining token window.
• This ensures data integrity and transparency – no silent alteration of the user's input.

------------------------------------------------------------------------------------------------------------------------

### ECONOMIC ADVANTAGES:
1. Cost reduction – Each cache hit saves $0.000X per call; at millions of queries, it's huge.
2. Latency reduction – 2 seconds → 10ms for repeated/similar questions.
3. Consistency – Same answer for similar questions (no model drift or slight hallucinations).
4. Scalability – Cache servers can handle 100x more QPS than LLM endpoints.

========================================================================================================================

NEW TOOLS USED COMPARED TO OLD PROJECTS:
1. FastAPI:
	Async, automatic OpenAPI docs, high performance, type safety with Pydantic.
2. Redis:
	Actual standard for low-latency exact cache, supports TTL, pub/sub.
3. Chroma:
	Lightweight vector DB, persists to disk, good for semantic search at moderate scale.
4. transformers:
	HuggingFace transformer that matches the current model and counts token precisely.
5. Docker Compose:
	Run Redis + Chroma + app with one command – essential for portfolio.
6. LangChain (OLD):
	Minimal use – only for LLM calls (abstraction to swap models) and message formatting.
7. Mock tests:
    We produce mock tests to verify the correct functioning of the project without external client servers.

========================================================================================================================

### TREE STRUCTURE OF THE PROJECT:
semantic_cache_proxy/
├── app/
│   ├── __init__.py
│   ├── config.py
│   ├── models.py
│   ├── main.py
│   ├── cache/
│   │   ├── __init__.py
│   │   ├── exact.py
│   │   ├── semantic.py
│   │   └── token_control.py
│   └── llm_client.py
├── tests/
    ├── __init__.py
    ├── conftest.py
    ├── test_exact.py
    ├── test_semantic.py
    ├── test_token_control.py
    ├── test_llm_client.py
    ├── test_main.py
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
├── .env.example
└── README.md

========================================================================================================================
========================================================================================================================

 =====================================
|| EXPLANATIONS OF THE VARIOUS FILES ||
 =====================================

REQUIREMENTS.TXT
We will use 5 categories of tools:
 1. Web API → Expose the cache as a service (REST endpoints) → FastAPI + Uvicorn;
 2. Caching → Store exact responses (Redis) and semantic embeddings (Chroma) → Redis, ChromaDB;
 3. LLM & Embeddings → Generate answers (Groq) and convert queries to vectors (local embedder) → LangChain Groq,
     sentence-transformers;
 4. Token Control → Count and truncate prompts to fit model limits → transformers.AutoTokenizer (initially used
     tiktoken, but it works ONLY for OpenAI, then moved to HuggingFace transformers to ensure consistency of the
     tokenizer with the LLM's vocabulary and perfect match);
 5. Configuration & Utilities → Manage settings, retries, logging → pydantic-settings, tenacity, loguru.
Why these are modern and production-ready:
 • FastAPI – async, auto-OpenAPI docs, Pydantic validation – used by Netflix, Uber.
 • Redis – industry standard for low-latency caching (used by Twitter, GitHub).
 • Chroma – lightweight vector DB, but can be swapped for Qdrant/Weaviate later.
 • Groq – LPU inference (very fast), has a free tier, hence used by some AI startups.
 • transformers – precise token counter matching the model for the exactness of the count.
 • loguru – prettier, more informative logging than Python's built-in.
Additional notes on this file:
 • uvicorn – ASGI server that runs FastAPI;
 • redis – In-memory key-value store. We'll use redis.asyncio for non-blocking operations.
    Stores the exact cache: (query_hash → response);
 • chromadb – Vector database. Stores embeddings of queries plus response metadata.
    We'll search for similar embeddings using cosine similarity;
 • langchain-groq – Official Groq integration for LangChain. Wraps Groq's API with the standard LangChain BaseLLM
    interface;
 • sentence-transformers – Library for sentence embeddings (from Hugging Face). We'll load all-MiniLM-L6-v2.
    This runs locally, no API needed, uses ~80MB RAM. It converts queries into 384-dimensional vectors for similarity
     search;
 • tenacity – Retry library with decorators. We'll wrap the Groq API call with @retry to handle transient network
    errors;
 • Huggingface transformers - they ensure the exact token count by matching the LLM model.
 • loguru – Modern logging with structured output, colors, and easy file rotation. We'll replace the standard
    logging module;

------------------------------------------------------------------------------------------------------------------------

### CONFIG.PY
In any production system, configuration must be separate from code. This allows:
 • Changing settings without modifying code (environment variables, .env file)
 • Running the same code in development, staging, and production
 • Keeping secrets (API keys) out of version control
1. About Redis (REmote Dictionary Server): we use Redis when a user asks exactly the same question (byte-for-byte same
     string), we want to return the cached response in ~10ms without going to Chroma or LLM.
    We use Redis in Docker: redis://localhost:6379 and we put this in config.py, instead of hardcoding, allowing us to
     switch between environments, for example Development and Production, seamlessly, without changing code.
    With redis_ttl_seconds, we decide how long cached responses live, otherwise we would endlessly grow our memory, but,
     without, we would lose the main purpose of caching for FAQ; TTL forces periodic LLM to refresh cache but keeping
     repetitive ones, at least temporarily.
     In our project we set it at one hour, however, being in config.py, we can seamlessly change it, in regard to
      production demand.
     Just to keep in mind, once the cache expires, next request will be a cache miss, forcing the LLM to give a fresh
      response.
     Possible Redis enhancements, may include "socket_timeout", "retry_on_timeout", "health_check_interval".
    With redis_max_connections, we establish a max in the connection pool size; if the limit is reached, the next
     requests will wait for a "socket" to open.
     It is written in config.py in order to then customize it, with the business needs in production.
     On a laptop, 10 max connections is fine; in a serious production environment, it can be easily scaled over 100.
2. About Chroma: Chroma is a vector database. In our semantic cache, we store a KEY, namely an embedding vector,
     and a VALUE, namely the original query text + the LLM response.
     When a new query arrives, Chroma stores and fetches embeddings generated using the local SentenceTransformer model;
      asks Chroma to find "the most similar stored embedding" through similarity search (for example cosine similarity)
      and, if "similarity score >= similarity_threshold", we get a cache hit, hence we return the cached response.
      Other vector embedding, eventually more powerful, are Qdrant or Pinecone or Weaviate.
     With chroma_persist_directory, it says to Chroma where to store its data to then load it from the given directory;
      this is consistent through restarts.
      In production, a Docker volume or a mounted permanent disk can be alternatively used.
      These settings are more easily configurable through the config.py file. The default location is a "./chroma_cache"
       local folder in the project root.
     With similarity_threshold, we define the minimum requirement to achieve the cosine similarity hit,
      therefore the cache hit. Empirically, 0.92 is the sweet spot for short queries (i.e. customer questions),
      balancing safety and hit rate. Must be a value between 0.0 and 1.0.
      Other values raise a validation error at startup.
     With embedding_model_name, it specifies the LLm to use for converting sentences/text into embeddings; the model
      runs locally and produces vector of fixed dimensions to feed for the similarity search. Good models to use,
      given existing benchmarks: all-MiniLM-L6-v2 (lightweight, ~80MB RAM, 384-dim vectors, with fast inference),
      all-mpnet-base-v2 (higher quality but bigger dimension 768-dim), BAAI/bge-small-e (good for retrieval).
     CONCERN: Unlike Redis, Chroma does NOT have a TTL (Time To Live), hence it can scale memory consuption scarily fast
      in production-like environments, deletion is managed by-hand by default (concern!). Can be "hybridized" into a
      Redis like setting, we explain possible strategies:
       2a. 1st strategy: Time-based Metadata + Periodic cleanup (consistent but objectove and non concept related).
         We store a timestamp, added entry in metadata, for each entry and run a background job (i.e. daily) to delete
         entries older than N days;
       2b. 2nd strategy: LRU (Last Recently Used), namely keep only last N entries (More complex, but more productyion
        solid).
         We store up to N entries and gradually delete older ones upon adding new ones;
       2c. 3rd strategy; Using more sophisticated tools.
         More complex but complete tools can be used intead of Chroma, namely Qdrant (support TTL), Weaving (supports
          time-based sharding and cleanup), Pinecode (can set delete by metadata filter);
     We used the LRU strategy, with semantic_cache_lru_check_frequency, hence the max amount of entries to be saved into
      the semantic cache. Nonetheless, the vector store can be simply changed later one without changin the logic
      (abstraction) of the project.
3. About Token control: once we hit a cache miss (exact and sematic cache check), the LLM is called and the input may
      contain the User query, additional context from the conversation and system instructions. If the total context
      exceed the max context window (N tokens for each model), the LLM may either refuse the request or truncate
      silently and unexpectedly; this is awful and disingenuous in production. Moreover, sinding long prompts wastes
      tokens (cost) and increases latency (slowness). A common way to assess this problem are a "smarter" truncation
      (namely keeping only the first X% and last Y% of the prompt), which still acts silently towards the User.
      There could be more stable and "ethic" ways for an honest and stable production-style solution:
       3a. 1st strategy: Alert and Reject (more critically exact for the User, useful where data loss in not acceptable)
         If token count > max_prompt_tokens, it does NOT call the LLM; but it reponds clearly stating to the User to
          shorten the prompt.
          The response will be like Return HTTP 400 with error: "Prompt too long (X tokens, limit Y)";
       3b. 2nd strategy: Smart sliding window (better for conversational systems where older context loses importance).
         The memory only keeps the most recent N tokens and drops older context, i.e. chat memory;
     We implemented three configurable strategies: reject, truncate_with_warning, summarize_overflow.
      The strategy is selected by overwriting the overflow_strategy field in config.py (default is reject).
     The Token control will:
       1. Counts tokens, measuring accurately prompt length through transformers.AutoTokenizer
       2. If len(prompt_tokens) > max_prompt_tokens, it will raise a HTTPException with details and NOT call the LLm,
           otherwise, the LLM call goes on.
4. About Perfomance: it will handle error management, namely:
     • request_timeout_seconds: float = Field(default=30.0, ge=1.0) - sets the max time (in seconds, this case 30) for
        the HTTP to send a response from Groq after sending the request. If the time exceeds, the request is canceled
        and an exception is raised.
        Good and customizable for production-grade scenarios.
     • retry_attempts: int = Field(default=3, ge=0) - sets the max retry attempts for a LLm request before giving up,
        and, therefore, raising an exception.
     • retry_backoff_factor: float = Field(default=1.0, ge=0.1) - controls the delay between retries, using the formula
            delay = backoff_factor * (2 ^ (retry_number - 1))
        hence:
            Retry 1, wait one seond; Retry 2, wait 2 seconds; Retry 3, wait 4 seconds
5. About Model Config: this class is the configuration for Pydantic's Base Setting, it controls how environment
     variables and .env files are loaded, and how extra fields are handled. In particular, it says where to look for
     environment variables. By default, it reads from os.environ.
    With env_files, it also reads from a .env file.
    The extra part will say how we define extra fields, namely if we ignore, warn or raise an error.
    The encoding ensures the .env file is read withUFT-8 (critical for non-ASCII characters).
    This is standard for professional Python projects.
    This layout ensures that secret data can be injected directly from private production settings, where secrets are
     stored.
    It is provided a .env.example dummy file with example values and instructions.
    If no .env file is found, the script simply skips it, no error, and it reads from system environment variables.

------------------------------------------------------------------------------------------------------------------------

### MODEL.PY - DEEP DIVE INTO PYDANTIC
Pydantic is a data validations and settings management library for Python, in particular for:
 • Declarative models - Defining expected structures with type annotations;
 • Automatic validation - Raises clear errors when data (types) do not match;
 • Serialization - Converts Python objects to JSON (and back cleanly and effortlessly);
 • Performance - Very fast computing (written in Rust v2);
 • Integration - It is used by FastAPI, LangChain, LangGraph, Hugging Face, etc...
Useful tools are:
 • BaseModel - A base class for creating Pydantic models. Inside this class, items can be more and more customized;
 • Field - Adds metadata and validation constraints to a field beyond its data type (string, int, etc.);
 • ValidationError - ValidationError raises detailed error messages if constraint conditions are not met;
 • model_config - Controls the behavior of pydantic models and can specify config options, for example
    "extra": "•••" - controls handling of extra fields, with 'forbid', 'ignore', 'allow' instead of •••, or
    "str_strip_whitespace": True - trims out the whitespaces from strings, or
    "frozen": True, makes the model immutable, or
    description="•" add a text description to the fields created inside the class, or
    "json_schema_extra": provides an example to feed the LLM (few shot prompt-ish);
 • ge=xx or le=xx amounts to fix constraints on the intervals values can be taken from, for given objects.
More classes in the same project can be created, depending on the use we make of them, for example, we will use:
  ChatRequest, ChatResponse, ErrorResponse, CacheStatsResponse.
A complete example, used in our project is in models.py. In this file, we defined the four classes stated above
  to then feed our semantic proxy cache Chroma build.

------------------------------------------------------------------------------------------------------------------------

### CACHE/EXACT.PY & FOCUS ON FASTAPI
This file defines the exact cache (Redis) implementation. This is the first cache layer,
  that will hit once the same question is repeated; in this case, the previous response is recycled, and, therefore,
  no LLM tokens (no cost) are used and the latency is critically reduced (~10ms).
  We recall that Redis looks in-memory, hence lookups take microseconds (low latency); it supports TTL (Time-To-Live)
  rules, so old queries expire automatically and, lastly, can be shared across multiple service instances, always
  natively, which makes it good for scaling into production-like complex systems.
Redis commands are similar to simple key‑value operations and are standard for query operations, and to manipulate
 relational databases.
 The operations used in this project are:
 • get(query, context) - GET to fetch the key if it exists, returning a cached response or NONE;
 • set(query, context, response) - to store the response with TTL (from config);
 • delete(key) (optional) – to manually DELETE keys in case of cache invalidation;
 • stats() – this returns the number of keys, useful for monitoring.
For storing a Redis keys, we compute a SHA256 has made of (context, query), this allows to keep keys short and
 deterministic.
 REMARK: SHA256 HASH: short for Secure Hash Algorithm 256-bit, is a member of the SHA-2 family,
  and it produces a unique 256-bit (32-byte) hash for any given input.
To not-blocking async operations, which is critical for FastAPI, we use redis.asyncio.
To store the response, we use plain text (plain text is enough in this project), but can be customized easily for
 example with more complex JSON format.
The TTL is set up from settings.redis_ttl_seconds, the default is 3600 seconds = 1 hour.
Lets us analyze the code:
 • def _make_key - used to create the keys to memorize for the queries; given they are made of strings (possibly long),
    we convert them to a fixed 64-char hex string (memory cost-efficient).
    We use a prefix "chat:*" in order to logically group the exact caches from other instances (for exmaplke semantic
     caches) or other keys.
    Moreover; if we add keys in the future, this setup will NOT make them collide with the exact caches ones,
    it will be needed to use another prefix in case (e.g. session:xyz789, key for user sessions, or,
    rate_limit:user:42, key for rate limiting for users). Lastly, this allows us to edit them or act with them,
    using them as a group: for example, we can delete them all at once, using the prefix; we can add other prefix
    levels, for example, chat:development:..., chat:quality:..., chat:production:...; it allows us to read them better,
    as the prefix will indicate whoever is auditing the code what are the keys about.
 • async def get - tool to retrieve with cache: it retrieves cached response for exact query+context and returns a
    response string if cache hit and None otherwise.
    We note that - logger.DEBUG - means that we log at DEBUG level, hence visible only when LOG_LEVEL = DEBUG.
 • async def set - tool that stores the response with exact caching, namely stores it with the 64-char hex string key,
    with its TTL, taken from setting (aka config.py).
    The response is stored as a plain string (enough for the project), but can be customized to be stored as a JSON
     output too.
 • async def delete - tool to manually delete an exact cache entry and returns True if the key existed, and, therefore,
    has been deleted.
    This is crucial on the managing of the project, allowing the dev to manually purge entries at will, with the key.
 • async def stats - returns the precise amount of exact cache stored; as the previous one, great for monitoring.
    We scan only for keys with our prefix "chat:*", for consistency.
    We note that this setup is consistent for a relatively small number of keys (~100k keys);
    for greater amounts it's better to use the logger at INFO level or a separate "manual" counter,
    this would be less clean as it would not naturally pair up with the TTL expiration setup.
    This choice with the stats part is good ONLY if we assume the cache rate is small enough,
    otherwise the run of the stats part could grow linearly, but yet, very fast, if there is a huge amount of users
    (mind TTL is NOT based on amounts, but on time!); so, it is good to keep this in mind, as a yellow flag, in case of
    serious scaling.
    Alternatives (more complex as non-redis native for TTL) are redis INFO (we need to subtract keys from other
    namespaces (non "chat:*" ones); create a counter, which has to keep into accounts the keys that have been deleted
    through TTL, as it does NOT trigger a decrement automatically.
    Still a limitation to keep in mind.
    REMARK: count=1000 - does NOT mean that it wiull count only up to 1000 keys! It is a HINT to redis on how many keys
     count for each "round":
     if there is 50k keys, it will iterate the command 50 times (with 1000 keys each lap), hence making 50 round trips
     back and forth to Redis.
     Still, Redis, may do more or less than the exact amount, it is not a rigid contraint.
     Note that setting the count too slow, means too many Redis round trips, hence very slow; too high count and Redis
     may have issues while building a huge reply, and, if unlucky, block, creating memory and performace spikes.
     Generally, 1000 is a reasonable default.
 • def __init__(self) - and - async def close - to start or end our connection pool to the FastAPI to Redis.
REMARK: FastAPI: FastAPI is a modern Python web framework for building APIs and its key features are:
 • Async: supports async def endpoints, enabling high concurrency (and, therefore, reduced latency);
 • Pydantic integrations: request/response validation, serialization and documentation;
 • High perfomance: reduced egenral latency.
 • Horizontal scaling: allows multiple istances to run on it simultaneously;
 • Vertical Scaling (more power, CPU/RAM-use, per instance): it can handle thousands of concurrent connections and,
    with univorn as additional tool, can handle multiple co-workers (tools), where, every worker has a separate process.
 This whole project is aimed at attacking the principal bottlenecks present in tools existing: LLM calls
  (expensive as end-month "token bill") and latency optimization, through cache hits. In bigger production projects,
  Chroma (good for dev environments) can be easily and smoothly swapped for a distributed vector DB, depending on needs,
  budgets and project needs.

------------------------------------------------------------------------------------------------------------------------

SEMATIC.PY
This is the second cache layer, after the statis exact cache through Redis, using Chroma.
 The concept is very similar, although, we work on sematic similarity, rather than exact match,
  still aimed at saving tokens and optimizing latency.
About Embedding Functions: Embeddings are numeric representations of your data that capture meaning in a form AI models
  can work with. They can represent text, images, and eventually audio and video.
 Chroma stores and indexes embeddings so you can efficiently search for similar content.
 You can generate them locally with an installed library or remotely through an API.
 Embedding functions can be linked to a collection and used whenever you call add.
Some notes about this file:
 • Class Initialization:
   ○ chromadb.PersistentClient - makes so that the client stores data on the disk and, if the directory does not exist,
      it creates it locally.
   ○ self.collection - creates or returns (if existing) a collection named "semantic_cache" and a value related to the
      cosine similarity, which is pretty standard, but can be customized or further refined.
      For Chroma there is NO explicit key prefix,
      like Redis, but it uses a collection as its namespace: we use "semantic_cache", meanwhile for the LRU,
      we will use "semantic_cache:lru", which is a proper key name (Redis uses keys, Chroma uses collections).
      This will keep the logic organized and readable.
   ○ SentenceTransformer - loads from cache the model to use for the semantic search (downloads it locally the first
      time);
   ○ decode_responses=True - means that it decodes the Redis response into UTF-8 format, instead of raw bytes,
      which would be the result if we do not mention the decoding, as the default is False. This makes sense as our
      project works with strings, not raw bytes, though, this only restricts it to being able to work with raw bytes
      (scalability concerns, in case);
   ○ self._insert_counter - sets a counter that gets increased by one every time that we add a new semantic cache entry
      (see async def add), useful to check size, in case, and evict, meaning removing data from a temporary, fast-access
      memory area, like a cache or session state, to free up space.
 • def _get_embedding - generates the embedding: encode returns a numpy array of floats, which then get converted into a
     list, which is what Chroma expects. Logging only at DEBUG level.
 • async def get - search for semantic similarity for an existing query.
    Given a query and a context, performs the similarity search (we set up before the cosine method): operates in these
     steps:
    1. Creates an embedding from the given query+context;
   REMARK on CONTEXT: this will be handled in the main.py file. To give a view of it, if a user starts a conversation by
    saying "I bought a laptop" and, then, saying "What about refunds?", the context would include the previous message
    too, relative to the current query. Represent history and instructions that should be considered as part of the
    cache content.
    2. collection.query(query_embeddings=[...], n_results=1) - Chroma returns the single (n_results=1) most similar
        result, comprehensive of its metadata (where response is and the original query) and distance
        (result of the 1 - cosine similarity, hence similarity = 1 - distance);
    3. if the similarity is greater of equal than the similarity_threshold, we return the cached query, saved in local
        memory.
       REMARK ON RESULTS: results, given from Chroma's collection.query(...) is a nested list, which can be generalized
        to far more complex results (matrix, namely results['metadatas'][query n°][result n° for that query]);
        in our case, we have only one query and one result for the single query, hence we do simply
        results['metadatas'][0][0].
    4. await self.redis.zadd - will update the timestamp and doc_id information about the semantic cache hit
       REMARK ON ZADD: zadd adds or updates a member in a Redis sorted set with a new score, called timestamp,
       we use it to track LRU order;
    5. it returns a triplet tuple made of (response, cached_query, similarity), so we know what has been matched with
        the current query, in cache we have a semantic cache hit.
 • async def add - to add and store a new (query, response) to our sematic cache, with LRU tracking.
    Once again, as for the search, we divide this tool in different steps:
    1. full_text - we create the tuple of context & query
    2. embedding - we create the relative embedding (to be fed to the similarity search algorithm);
    3. doc_id - we attach to the entry a unique identifier, that we call doc_id;
    4. self.collection.add - we store it into Chroma, with its id, embedding and metadata
        (query&response&creation time);
    5. with zadd (see above for details), we add the LRU information to Redis, it will be the creation time in this
        case;
    6. self._insert_counter += 1 - we increase by one the counter for the times we used that sematic cache;
    7. semantic_cache_lru_check_frequency - we check if we reached the max amount of semantic cache to keep in memory
         (limit=max_semantic_cache_entries, adjustable in config.py); if the number of entries is greater than the max
         we decided to allow, we call the method _evict_if_needed, which we explain in the next paragraph, which checks
         the LRU times and deletes the oldest entries into our semantic DB, so that we stay below the agreed limit
         (max_semantic_cache_entries).
        Lastly, we reset the counter, so that we can re-check and, eventually, re-purge the DB after an other N entries
         added.
    8. logger.debug - lastly, we add, at debug level, the log of what we did.
 • async def _evict_if_needed - we define the method to delete all the oldest (LRU-wise) entries, so that we have at
     most max_semantic_cache_entries-entries into our semantic cache DB.
    In terms of logs, we keep:
    ○ the before-deleting semantic cache size, the number of entries deleted and the resulting sematic cache DB size at
       INFO level;
    ○ which semantic cache entries we delete (exact doc_id identification) at DEBUG level;
    ○ which semantic cache entry (doc_id) has NOT been able to be deleted and the specific Exception (error) text.
    REMARK ON ZPOPMIN: this command removes and returns the member(s) with the smalled "score" from a generic Redis set.
     If the score is computed by timestamps, the smallest means the oldest. This method is called "atomic",
      meaning the removal and return of the parsed and edited solution is done with the same command.
     In our case - popped = await self.redis.zpopmin("semantic_cache:lru", count=1) - will return a list of tuples,
      made of (member, score).
    REMARK ON THE VARIABLE POPPED: we store the deleted tuples in the variable "popped"; the if check is a safety check:
      if the popped variable is empty, it means an other process or a manual operation already edited the set and
      the loop breaks safely, returning a empty list. (if not popped - means - if popped is (an) empty (list)).
     An other thing to note is that, on the uniqueness side, we are safe: zpopmim would delete identical value, in case
      of multiple entries with the oldest score;in out code this does NOT happen, as we are using timestamps,
      so that's ok.
     Semantically, the variable name "popped" is reasonable, as .pop() is a Chroma-standard method to delete entries
      from collections during data processing.
    REMARK ON ZCARD: this acting like the cardinality operation in a given set ans returns an integer of
      how many members are present in a ordered set. In our case, we use it to count the size of our current semantic
      cache and to compare it with "max_semantic_cache_entries", in order to decide if the eviction process is needed
      ("collection.delete"). In this case, zcard would give the same result as using collection.count() (in Chroma)
 • async def stats - returns us semantic cache statistics, in particular: the number of documents in the Croma
     collection, the number of entries in the Redis-LRU recording (those two should match, if the counting is
     consistent); if they do not match, we raise an Error and detail what is missing where, to be able to then assess
     the problem and question Chroma and Redis.
    It returns a dictionary with all the information's about Redis, Chroma or, in case, the errors found across the
     two DBs.
 • async def close - Closes the Chroma client for consistency.


------------------------------------------------------------------------------------------------------------------------

TOKEN_CONTROL.PY
It manages all token-related operations for prompts, before they are fed to the LLM;
 in particular it supports these strategies, depending on the method chosen in settings.overflow_strategy, from
 config.py.
 1. reject - Alert & Reject: if a prompt exceed the max_prompt_tokens (see config.py), it raises a
     TokenLimitExceededError Error with a clear message;
 2. truncate_with_warning - Truncation with warning: it automatically shortens the prompt, keeping only the first X% and
     the last Y% of tokens, dropping the middle. Good if an answer is always required, despite the cost of risking
     losing context. Returns metadata containing clear warning messages telling that the prompt was altered;
 3. summarize_overflow - Keeps part of the prompt (X% of the allowed limit, in terms of tokens), and sends the remaining
     part (the overflow) to a cheap LLM for it to be summarized. The summary is then prepared to be used.
     A log at warning level is therefore produced (TO USER????????? !TODO!)
REMARK-DEEP DIVE ON THE LANCHAIN IMPORTS: We import two langchain modules we have not used before:
 1. "langchain_groq" - the LangChain wrapper for Groq's LLM, used only for the summarization part in the
      summarize_overflow strategy;
 2. "langchain_core.messages" - which provides classes that manage the fow of messages between user and LLM,
      serving as backbone for the conversation. The main classes are:
      • HumanMessage - (the one we use), represents messages sent from user to LLM and allows customization of query,
          context and, optionally, metadata;
      • SystemMessage - defines instructions, like behavior, for the LLM (e.g. "You are an HR assistant";
      • ChatMessage - generic class that can represent any type of message (user, AI, or system),
          used to manage conversations that are made of any between user, LLM or system altogether;
      • ToolMessage - a class used to interact with external tools, like APIs and DBs;
We go through the code piece by piece:
 • class TokenLimitExceededError - this class defines the behavior in case of a Token Count Exceeding error,
     providing a clear Error message, comparing max_tokens (we will use the value set up in config.py,
     hence easy customization towards client needs) and token_count, given by transformers.AutoTokenizer;
 • class TokenValidator - this class initializes the token validation, meaning it calls the cheap LLM for the
     summarization, setting temperature (at 0.0, fully objective towards the text fed to the LLM), max tokens
     "max_tokens" for the summarization (we set up the default 512, as an happy medium, but we, as always, make it
     customizable in config.py), as well as the tokenizer and summarization model (MUST be the same, see config.py),
     last, reports when the summarization began with a log at INFO level;
 • def count_tokens - the function returning the token count
 • def _truncate_sliding_window - this works as follows:
     ○ we take the computed number of tokens and, if the token count is below the limit we chose in config.py,
        namely "max_prompt_tokens", we close the cycle and specify that we did NOT truncate anything and
        return everything unchanged; otherwise, we move on;
     ○ we count the token to keep both from the beginning and the end and rewrite the prompt as truncated_tokens, last,
        we decode (reconvert) back it into a string and count the truncated tokens with "truncated_count";
     ○ we log the truncation at INFO level, making it visible;
     ○ lastly, we return the truncated prompt and its metadata (if we truncated, token count before truncation,
        token count after truncation, strategy used in TokenValidation and a clear warning message (if we truncated).
 • async def _summarize_overflow - this works as follows:
     ○ we take the computed number of tokens and, if the token count is below the limit we chose in config.py,
        namely "max_prompt_tokens", we close the cycle and specify that we did NOT truncate anything and
        return everything unchanged; otherwise, we move on (as per the truncation method);
     ○ we keep the first "keep_tokens" and classify the latter as "overflow_tokens", last we decode them back into
        string (text);
     ○ we invoke the weak LLm prompting a under 200 word summary of the "overflow_tokens"'s text,
        decode it back into string (text) and save it as "summary". Hence our prompt will be:
        processed_prompt = kept_part + marker that summarized content starts + summary
     ○ we count the token for the new processed_prompt (for completeness)
     ○ last, we log the result at INFO level, the processed_prompt and its metadata (if we did the _summarize_overflow,
        original token count, new token count, name of strategy used and a clear warning message (if we summarized).
        Consistent and similar to the truncation case.
 • async def prepare_prompt - single entry point that, given any strategy or method used, just returns
     (processed_prompt, metadata), where, of course, the metadata depends on the strategy used. This works as follows:
     ○ we take the computed number of tokens and, if the token count is below the limit we chose in config.py,
        namely "max_prompt_tokens", we close the cycle and specify that we did NOT truncate anything and
        return everything unchanged; otherwise, we move on;
     ○ depending on the strategy used (if-elif paragraph), we return the correct prompt and metadata
     ○ if the strategy used does NOT exist, simply returns Reject and raises an Exception
        (no silent ignores and clear error messages)
 REMARK: TOKENIZER: The tokenizer is a Hugging Face AutoTokenizer loaded from settings.tokenizer_model_name.
  For consistency, it HAS TO BE the same used by the summarizer and (better even) the same tokenizer used by the
   main LLM (maybe a lighter version), because different models tokenize the same text differently.
  This allows the given LLM to see the exact token count (in its metric).
  We note that the tokenizer gives you the ONLY accurate (and objective for the give LLM) way to count.
  It is crucial to note that token is NOT equal work, for example, for some models, “hello” might be one,
   “hello world” might be two but “helloworld” could be three.

------------------------------------------------------------------------------------------------------------------------

LLM_CLIENT.PY
This script is the pure LLM caller that communicates with the given LLM (we chose Groq for this example, though,
  easily customizable, if the wanted model supports our tool, see "GENERAL NOTES AND HEADS-UP FOR LEARNING").
 The script assumes the prompt is already correctly edited to meet all token constraints (see token_control.py) and,
  given a prompt string, through a retry logic, for robustness and handling possible errors, responds with a string.
 We separate the three parts, to make it easily scalable and customizable (for example switching LLM) and
  completely independent of the cache and token logics, handled respectively in exact.py & semantic.py and
  token_control.py.
 The main import tool we heavily use is "tenacity", its decorators allow us to, respectively, handle the retry logic
  with "retry" handle the timeout after X attempts with "stop_after_attempt", handling wait time between attempts,
  increasing it between each attempt (common practice, even by major cloud providers, as a flood of immediate retries
  can make the problem worse) with "wait_exponential", retrying under specific conditions with
  "retry_if_exception_type", raising an error if all the given retry logics fail with "RetryError".
  The exception types are handled by "httpx".
Some notes about this file:
 • class LLMClient: def __init__ - initializes the LLM model, wìth all its setup (api_key, model, temperature,
     max_tokens, timeout) taken from config.py. Moreover, it reports the LLM call and the model used with a log at
     INFO level;
 • @retry - defines the retry method. It says how the retry logic will work, after calling the LLM:
    • it stops after settings.retry_attempts attempts (in our case, three);
    • it waits wait_exponential after each attempt, where the waiting is capped between 1 and 30 seconds, and it
       increases, attempt after attempt, with an exponential factor: 1.0 * (2^(1-1)) = 1.0 sec after 1st attempt,
       1.0 * (2^(2-1)) = 2.0 sec after 2nd attempt, 1.0 * (2^(3-1)) = 4.0 sec after 3rd attempt,
       1.0 * (2^(4-1)) = 8.0 sec after 4th attempt, 1.0 * (2^(5-1)) = 16.0 sec after 5th attempt,
       1.0 * (2^(6-1)) = 32.0 sec, which we constrained at 30.0 sec after 6th attempt, from the 7th onwards,
       the wait time is capped at 30 secs. This avoids ridiculous long waits, even if the service is down for 1h.
       This strategy is provided by the "tenacity" library; it is very used because:
        1. prevents overwhelming the host of the service (the LLM host);
        2. allows progressive time for recovery, in case the system is, just temporarily, malfunctioning;
        3. standard practice used by major cloud providers, such as AWS.
    • it tells the function to only retry on certain errors, for example, is the error is a TokenLimitExceededError,
       it will NOT go through the retry cycle, as the error will NOT stop persisting.
       Only applies to network/timeout/HTTP errors.
 • async def _call_llm_with_retry - actually sends the prompt to the LLM provider, in this case Groq (via LangChain)
     and returns the response string. It is different from the "generate" method, as it is a provide method that
     contains the retry logic as well (@retry decorator), keeping the retry logic autonomous and more easily testable.
    This method is defined as an async operation, as it calls await self.llm.ainvoke(messages), an asynchronous
     operation which does NOT block the event loop while waiting for the LLM to respond (less latency and more agile).
    With messages = [HumanMessage(content=prompt)] we fetch a list of messages (in this case just the given prompt is
     enough) to feed to LangChain, where HumanMessage represent a user message (role = "user").
 • async def generate - calls the retry-method defined above, with no extra logic and forces the response to be returned
     as a string. The other logics, like truncation and token validation and caches, etc..., are handled in the
     main.py file.

------------------------------------------------------------------------------------------------------------------------

MAIN.PY
Provides a FastAPI web server that exposes the semantic caching proxy for LLM queries, handling requests, checking
  exact & semantic cache, applying token control methods (rejection, truncation, summarization), calling the LLM,
  storing cache responses and returning structured responses, comprehensive of metadata
  (token count, cache hits, warning).
 Moreover, it provides admin endpoints for cache operations and health checks.
 After recieving a single request, it works as follws:
  1. Recieves the query, aka ChatRequest, (together with context, if present);
  2. Calls the token validation process, eventually generating TokenLimitExceededError (= HTTP 400), or, otherwise,
      if everything is clear, returning the (eventually) processed prompt + adjacent metadata;
  3. Checks the exact cache DB (Redis) and, if there is a cache hit, returns, from the DB, the response stored with
      cache_hit;
  4. Checks for the semantic Chroma (Chroma), similarly to Redis, if there is a cache hit, returns, from the DB,
      the response stored with cache_hit;
  5. Creates the prompt, made of query + context (eventually);
  6. Calls the LLM client to generate a response, or may raise timeout/network errors and proceed with the retry
      (eventually stoppage) errors;
  7. Stores the response in both cache BDs (exact and sematic);
  8. Returns the response, aka ChatResponse, with the additional information (token usage, cache miss metadata,
      and any eventual truncation warnings).
 Other useful endpoints for the application are:
  • GET /cache/stats - returns information and details on cache status;
  • GET /health - returns hhealth status check results to allow other orchestration tools (like Docker or Kubernetes)
     to check whether the service is running correctly and is ready to accept data. If everything is running correctly,
     it returns {"status": "ok"} (= HTTP 200); if the service is facing mulfunctioning, the probing fails.
Next, we explain how it works:
 • we get the ChatRequest main information (start_time, query and context), with which we build the prompt
    (query +  context) and the shutdown process data (like end_time), other than closing Redis and Chroma connections;
 • we create a FastAPI App with the related metadata shown in /docs;
 • we perform a fast token count check: if we surpass max_prompt_tokens, we stop the process and return tu the User
    why the operation stopped, namely wity a log at WARNING level and a JSON response saying that we met the error
    "TOKEN_LIMIT_EXCEEDED" (no LLM call in this case), otherwise, we move forward;
 • exact cache check: if we get a exact cache hit, we return immediately (no LLM call) the exact_reponse and some
    additional data, like the tokens saved etc...; otherwise, we move forward;
 • semantic cache check, exactly as for the exact cache, but with the semantic cache method, logic is the same,
    if we get a cache hit, we do not waste tokens as no LLM call happens;
 • cache miss → build prompt: if cache methods fail (cache miss), we build the prompt and, if needed, we apply the
    summarization or truncation methods, with prepare_prompt, and return the changes detials with the ErrorResponse
    and a log at WARNING level.
    If none of these methods are needed, we move forward;
 • we call the llm and report if there is any connections error, logging them at ERROR (blocking) level.
    If there is no errors, we store the content of the prompt in the respective exact and semantic cache DBs.
     Mind this is a "fire-and-forget" caching, namely it does NOT stop the response, improving latency.
    Then, we generate, through the LLM, the reponse and highlight some techincal detials such as latency,
     prompt_tokens and reponse_tokens and log at INFO level.
    Last, we generate the response, with ChatResponse, with all its technical metadata;
 • @app.get("/cache/stats" - returns the current cache size sizes and configuration, useful for monitoring;
 • @app.get("/health") - returns the health check informations we explained before, liveness check for orchestrators.

------------------------------------------------------------------------------------------------------------------------

DOCKER-COMPOSE.YML
Defines and runs the entire application, using Docker Compose. Docker Compose is a tool used for defining and running
  multi-container Docker applications. It allows you to configure your application's services, networks,
  and storage volumes in a single configuration file, then launch everything simultaneously using a single command.
 It includes:
  • Redis (exact cache) service - exposes port 6379 internally and uses official Redis image.
     The Redis is exposed to the host in development,
  • app - the project we have built until now, about the FastAPI token control and exact&semantic cache proxy.
     It exposes port 8000 to the host.
 This setup simplifies local and eventually production development (command "docker-compose up"), ensures consistent
  setup environments (like Python version, file dependencies or Redis version) and organizes the project.
 It loads environment variables from .env files, like the API key, ensuring secrecy for the API key for example.
 It has volumes for Chroma persistent storage, ensuring consistency, even upon restarts.
  The Docker creates the chroma_cache directory automatically, if not present, ensuring it has write permissions inside.
 It is important to note that the health check applies only to Redis, given Chroma runs in the container present
  locally inside the app.

------------------------------------------------------------------------------------------------------------------------

DOCKERFILE
Creates a lightweight reproducible image for the project, installing system dependencies, setting up a Python
  environment, copying the application code and installing the necessary packages for the code to run and, lastly,
  defining the command to run the FastAPI server using univorn.
 Univorn is a ASGI (Asynchronous Server Gateway Interface) web server, built from the ground up to handle modern,
  asynchronous (async/await) Python code and frameworks like FastAPI, Starlette or newer versions of Django.
  It is the backbone environment that boots up a Python project, making it accessible on the internet.
Concerning security, this file:
 • copies dependencies and requirements from requirements.txt, caching requirements;
 • uses curl to do all the health checks described before, for example it can tell Docker: "Every 30 seconds,
    try to load the website at http://localhost/health. If it loads successfully, the container is healthy. If it fails,
    mark it as unhealthy.";
 • runs on four workers, but can be manually adjusted. In Docker, workers mean a separate Python process spawned by
    Uvicorn to handle incoming HTTP requests concurrently;
 • writes, through docker-compose.yml, the semantic cache in /app/chroma_cache, a host directory, so that data persists.

------------------------------------------------------------------------------------------------------------------------

.ENV.EXAMPLE
Provides a template for all the environments variables needed to run the project. The User can copy it and fill it with
  the actual values, with secrets like API Key. This file is NEVER committed to version control,
  ensuring secrecy of the values inserted, keeping secrets outside of the codebase.
 It highlights which variables are required and which are optional.
 This setup allows the User to use the application and set it up in different environments, like Development,
  Staging and Production, ensuring the data is well organized
 The clear separation between required (only API key) and optional variables, make it easy for the User to de-comment
  a piece and overwrite the default.

------------------------------------------------------------------------------------------------------------------------

README.MD
This file is related to the comprehensive documentation and high-level explanation of the project, of the objectives
  studied with the project and about the goals achieved. As well as the possible future implementation, scalability
  and next projects.

========================================================================================================================
MOCKING (TESTS) FOLDER
========================================================================================================================
CONFTEST.PY
This file will mock the config.py settings without reading (truly) the .env, but producing results as if it was read.
The same concept will apply for a AsyncMock for a Redis client connection and a Chroma collection, all through the
 pytest library, the testing framework. MagicMock is used to mock synchronous methods, instead.
 The Settings class taken from app.config is the real one from which we create a mock instance of, using the decorator
  @pytest.fixture. The same we do for the Redis client and the Chroma collection.
 The - .fixture - means that, given the pytest module (the library), there is a function called fixture, hence it is
  executing pytest.fixture(your_function), in the first case, pytest.fixture(mock_settings).
  This function works as follows: it wraps into an internal object called FixtureDef, which stores:
  • the function itself, in this case mock_settings
  • the name of the fixture used, in this case always mock_settings
  • the scope, which can be function, module, session, etc... In this case it is function (the default)
    REMARK ON SCOPE
     Pytest manages FixtureDef objects using five standard scope levels, ordered from shortest to longest lifespan:
      1. function: The default scope. Pytest runs the fixture fresh for every single test function that requests it.
      2. class: Runs once per test class. All test methods inside that class share the same fixture instance.
      3. module: Runs once per Python file (.py). Every test inside that file shares the same fixture instance.
      4. package: Runs once per directory package. It is shared across multiple test files within that folder.ù
      5. session: Runs exactly once at the very start of the entire test run. Every test in the whole suite shares it.
  • dependencies, if there is any
  Pytest keeps an internal registry (made of a Python dictionary) of all available fixtures and, every time a new
   FixtureDef is ran, it adds it to the registry, using the function name as a key. Lastly, it attaches a special
   attribute, so that pytest can identify it later.
  When running the code, the Pytest will look for this function inside the Fixture collection and run it, ensuring
   more smooth and precise execution.
 MagicMock(spec=Settings) - creates a mock that pretends to be a Settings instance. If it tries to reach an attribute
  which does NOT exist in the "real" settings class, it will raise an AttributeError, like saying
  "I cannot mock what doesn't formerly exist!".
 We then manually set up all the attributes in the class, so that upon a test run, everything is visible and seemingly
  coherent. TODO! Later, in ????? we use patch to replace the former settings class.
For the redis client, we work similarly, where
 • mock = AsyncMock() - means that all the methods (hence mock.??) are Async methods by default
    It is critical to remember that, the Redis method we developed, either returns the cache hit, hence a string or, in
     case there is a cache miss, it returns None; we, therefore, "hardcode" None as a response from the Redis method,
     confgurable for different test cases. In the set method, we return None, as we do not care about the return value,
     as, given the get will return None, there is not interest in what we set in the memory, we just care that the
     method was called. Similarly, - mock.delete.return_value = 1 - means that, once calling the deletion method, it
     will always return bool(1) meaning True.
 • scan_iter - from "app/cache/exact.py" there is the method "self.redis.scan_iter", which, in real Redis, it iterates
    through all keys matching a pattern, namely "chat:*". In our mock setup, we define a fake async generator that
    simply yelds one hardcoded key, namely "chat:abc123"; this ensure that the loop works (iterating once only),
    confirming the count logic works.
 As before, we will force pass this mock to redis.from_url via the patch command, to implement the imposed mock
  responses.
For the Chroma collection, again it is similar, where
 • query - returns by default an empty result, indicating cache miss. This is easily configurable to simulate cache
    hits, by hardcoding the query.return_value object.
 • the - add, count, delete, operations will be basically non-existent, forcing them to do nothing, again configurable
    from here to hardcode results.
These fixtures will be valid and working in all test modules inside the /tests directory.

------------------------------------------------------------------------------------------------------------------------

TEST_EXACT.PY
This file serves for the generation of tests of instances replacing the original processes in exact.py, testing all its
 functionalities.
We use the mock_redis_client defined in conftest.py for the Redis client connection.
These tests will verify:
 1. The key generation, through _make_key function.
 2. The get method, both for a cache hit and a miss.
 3. The set method, both for a cache hit and a miss.
 4. The delete method, returning True or False, based on the Redis delete count, namely if there is actually something
     to realistically delete.
 5. The stats method, which counts the number of keys saved, using scan_iter.
 6. The close methods, to close the Redis connection.
These tests will ensure the complete and comprehensive functionality of the Redis tools, making sure that, upon serving
 identical prompts, same responses are produced, reducing latency and zero-ing the consumption of tokens.
We analyze how the code behaves, method by method:
 • test_exact_cache_make_key - verifies that the _make_key actually generates (correctly) a SHA256 hash with prefix
    "chat:". We mock Redis to ensure it does not actually create a real Redis connection (hence we dont need to mock the
    from_url function; the idea is that ExactCache.__init__ would try to connect to Redis otherwise.
   We use " 'app.cache.exact.redis' " to replace the redis module inside app.cache.exact with a mock version
    (using the patch); we do not use the clean mock_redis, because it would still try to create a Redis regular
    connection.
   Once we create a instance of ExactCache, inside cache, this method will not attempt a real connection, thank to the
    patch setup defined; then we create a key with the _make_key method.
   Last, we use the assert methods to make sure everything was done correctly, hence that key begin with our prefix
    "chat:" and that the length is correct, hence the 5 characters for "chat:" + the 64 characters from the SHA256.
    If the assertion returns negative, means there is an internal code design problem with the workflow.
 • test_exact_cache_get_hit - we simulate a cache hit and expect a string as a return.
   The setup about the fake Redis connection is the same as before.
   We mock the default value given by get (which would be None) with an handmade "cache response", to return a proper
    string.
   With the patch we replace only the from_url method, to redirect it to our fake Redis connection, this way, when
    the ExactCache runs, the ExactCache.__init__ calls the usual redis.from_url(), but receives our mock client instead
    of the real Redis one.
   Later, we create the cache instance to, then, create the response, taken from the mock Redis client.
   Finally, we move to the asserts, namely the checks, and we check: if the response was the mocked one and if the get
    method on the mock client was actually called exactly once, if there was no call or more than once, means there is a
    problem
 • test_exact_cache_get_miss - in similar fashion as the cache it, we check if the cache misses work as intended.
   We mock the return value of the get method to None (our current default in configtest.py, simulating a proper cache
    miss.
   As before, we mock the Redis connection with the patch command.
   We proceed with the call to the mocked Redis client as before, expecting to receive no results.
   Last, we check the functionality by asserting if the result of the call was None.
 • test_exact_cache_set - verifies that the set method works correctly, with the intended TTL.
   The beginning is always the same, mocking the Redis connection.
   We call the set method, which internally calls mock_redis_client.set, hence sets in our mocked Redis client.
   We assert if the set method (taken from the "original" code is called exactly once, which is what is expected.
   Recalling that, in exact.py script, we defined ex=settings.redis_ttl_seconds, taken from config.py, we check that the
    arguments passed to the set call are correctly taken from settings(=config.py).
   It is important to remind that, when we do - await cache.set("query", "context", "response") - internally, we create
    a key with _make_key, with query "query" and context "context" strings, then, we set the value "response" that WOULD
    be set in a real Redis and ex, namely we start the life-timer, taking it's lifespan from settings.redis_ttl_seconds.
    REMARK ARG AND KWARG: when we say - args, kwargs = mock_redis_client.set.call_args -, we are unpacking a tuple
     (args) and a dictionary (kwargs), for example, in
            await mock_redis_client.set("my_key", "my_value", ex=3600)
     we have args = ("my_key", "my_value") and kwargs = {"ex": 3600}, but we will check only for the TTL value to be the
     correct one set up in the settings(config.py) file, hence, in our case, 3600 (customizable in the given file).
     The call_args method stores the most recent call to the mock, with the explained values in args and kwargs.
 • test_exact_cache_delete - tests the correct functionality of the delete method (in case there is a key deleted).
   The setup is the usual, with the mocked Redis connection.
   We call the return method and store the metadata response inside of "result". It is important to note that, in
    conftest.py, we set up mock_redis_client.delete.return_value = 1, hence the delete method in ExactCache will return
    bool(delete_count), though in our mock test, we set up the delete_count to be 1 (namely, for the original code,
    there has actually been a key deleted (otherwise, would be delete_count=0), hence result=bool(deleted_count)=True
    (otherwise, would be result result=bool(deleted_count)=False).
   We, finally, check that the Redis delete method was issued exactly once, as intended, through the assert.
 • test_exact_cache_delete_not_found - same logic as the previous test, but to check if everything is correct once there
    is a cache miss.
   We set up delete.return value, namely the number of keys delete from Redis, to 0; therefore the bool(delete_count)
    will be equal to bool(0), hence False. Same check as before apply.
 • test_exact_cache_stats - verifies that the stats method counts keys correctly; from conftest.py, we set up the
    scan_iter to return, in the mock Redis client, only the key named "chat:abc123", hence we expect the dictionary
    stats to have only the expected key.
   We, finally, check the correctness of the key in the dictionary stats and that the number of items in the dictionary
    to be equal to 1.
 • test_exact_cache_close - checks that the closing of the Redis connection works correctly.
   We try to close the Redis connection through await cache.close() and check if the Redis connection method was called
    only once, as expected.
REMARK ON THE PATCH COMMAND: tests the correct functionality of the delete method (in case there is no key to delete).
 Patch is a context manager provided by the unittest.mock module in Python's standard library; it temporarily replaces a
  target object (anything between a function, class attribute or variable) with a mock version of the latter, usually in
  a testing environment.
 It serves to isolate the "clean" code to the tests made, with their external dependencies, still preserving the main
  functionality of the given code; acts like the decoratos we described in this file. This makes tests more fast,
  non-deterministinc (no "RNG"), free of charge (no API costs) and consistent (no "natural" network errors, unless
  specifically setup to simulate any of them).
 Internally, for example in - with patch('app.cache.exact.redis') as mock_redis: -, it replaces the target attribute
  redis inside app.cache.exact with a new MagicMock object, in this case mock_redis and returns the new mock-binding of
  the "fake" attribute mock_redis inside app.cache.exact. The logic of using "with" is very smooth, as it restore the
  original atribute redis once the with block finishes, making the code very fluent and robust, with respect to the
  former logic.
 It is worth noting that patch comes from unittest.mock, as said, part of Python standard library, not ONLY for pytest;
  in particular, pytest has no special integration with a patch. It rusn with non-mock object too, replacing the target
  object with a "new" parameter, for example:
        with patch('app.config.settings.max_prompt_tokens', new=10):
  It works with other libraries and test frameworks such as unittest, pytest, nose, doctest.
REMARK ON LOGGING THE ASSERTION RESULTS:
 There is no reason to add a logging logic to the assertions because, when an assertion is not met, hence the condition
  is False, pytest raises an AssertionError and a report about why it failed. This is NOT a log message, but a runtime
  exception that stops the test execution. The assert statements are per-se a tool handling the error and issues
  reporting.
REMARK ON PYTEST.MARK.ASYNCIO

------------------------------------------------------------------------------------------------------------------------

TEST_SEMANTIC.PY
We move on to the tests of the SematicCache class, which uses Chroma as the vector database, Redis for the LRU
 (not native in Chroma), storing documents IDs with their corresponding timestamps and SentenceTrasformer for the
 generation of embedding locally. We mock all the dependencies to avoid any latency and network constraints and
 problems. The LRU logic check is crucial, as, if it does not work correctly, it would cause indefinite memory growth.
These tests will check:
 1. The get method for the sematic cache hit.
 2. The get method for the sematic cache miss.
 3. The add method to store a new entry Chroma collection and set the LRU Redis.
 4. The _evict_if_needed eviction method for when we cross the limit for when the LRU cache size exceeds the limit.
 5. The stats method which returns the number of keys and consistency flags.
 6. The close method which closes the Redis and Chroma connection.
We analyze how the code behaves, method by method:
 • test_semantic_cache_get_hit - we check that, upon a cache hit, the returned response is correct and the LRU stats are
    updated correctly.
    We create an AsyncMock for the Chroma collection.
    We mock the query.return_value and simulate the result with a list of lists of IDs made of only one result
     [['doc123']], we set the distance between the query of the mocked cache hit and the result to be 0.05 and, then,
     with (cosine) similarity equal to (1-0.05=) 0.95 and, lastly with mocked metadata made of the original query input
     and the cached response.
    Next, we mock the SentenceTransformer.encode so that it returns a dummy embedding and, then, converting the numpy
     array into a list, which is expected by Chroma. This mocks the embedding = self.embedder.encode(text).tolist() in
     semantic.py.
    Later, we patch the Chroma client, the SentenceTransformer and the Redis client (as in test_exact.py) with
     mock_chroma_client, mock_embedder and mock_redis_client and we create our mock instance, where all the dependencies
     are made of mocks.
    We set the similarity_threshold to be 0.90, so that 0.95 (the one in our mock) is above it and, therefore, we should
     actually get a cache hit.
    Lastly, we begin the assertions for:
     ○ the cached response to be the mock one, namely "cached answer";
     ○ the cached query to be the mock one, namely "original query";
     ○ the sim(ilarity) computed to be the mock one, namely 0.95;
     ○ the zadd method to correctly update the LRU time on the cached prompt (query + response).
 • test_semantic_cache_get_miss - we check that, upon a cache miss, no cached response (nor any response) is returned at
    all and that no LRU updates have been done.
    Most of the setup is similar to the cache hit case, just return_value of the Chroma collection query, now, is fully
     empty, namely empty lists.
    We mock a embedding, as before, into a list, as well as all the patches listed above.
    Lastly, we do the assertions to check if:
     ○ response, cached_query and sim(ilarity) are empty (equal to None);
     ○ the zadd method has been called (we expect it not to be called).
 • test_semantic_cache_add - we check that the add method to the Chroma collection works as intended. It is important to
    note that the add method returns, by original method construction, three None, hence, we verify the internal works
    to act as intended.
    As always, first, we mock the Chroma collection, embedding operations and patch the usual elements.
    Then, we add the dummy data ("query", "context", "response text"), with the add method.
    Right after, we assert that the add method has been used once and same for the zadd (LRU first writing) method.
   REMARK ON CALL_ARGS(): every Mock (and AsyncMock) method has an attribute called call_args, which stores the
    arguments from the most recent call to the given mock. When we do
    - add_args = mock_chroma_collection.add.call_args[1] -, we are assigning the keyword arguments to add_args,
    therefore, add_args is now a dictionary containing all the parameters passed to Chroma through the original add
    method.
   Later, we assert that the "metadata" argument was actually passed  to Chroma and, hence, exists in the add_args
    dictionary, namely that the structure passed is correct. In our tests, we add only one document, hence
    - add_args['metadatas'][0] - fetches the first (and only) metadata dictionary.
   Last, we check that the three fields in the metadata that we set, namely "query", "response" and "created_at" have
    actually been passed to Chroma, in this test we check if they have been stored into add_args (we call it metadata
    for simplicity)
   Can be noted that we do not check the values (because we manually set them into the mock), but that the passing to
    Chroma through the add method worked correctly.
 • test_semantic_cache_eviction - we test the eviction method, which removes prompt entries inside the Chroma
    collection, once max_semantic_cache_entries is surpassed; we note that, in the original project, we call this method
    every time we add a new semantic cache entry into our Chroma collection (see sematic.py).
    As always, first, we mock the Chroma collection and embedding operations.
    We, then, mock the value that would be returned by zcard (which returns the cardinality of the set of the entries in
     the Chroma collection) to be a large number ("15˙000"); next, we mock the zpopmin return, as the oldest document,
     the document with id "doc_to_evict" andf timestamp "123456789"), namely we force the search of the odest document
     to be one we mock.
    Then, we do the usual patches, as well as we patch the max_semantic_cache_entries to be 10˙000 and
     semantic_cache_lru_check_frequency to be 1, hence we check every new entry added (we do not need to add thousands
     for our mock tests, one is enough, given we tested the add method as a standalone too).
    As it would happen naturally, we call the add method (with mock values
     ("q", "c", "r") = ("query", "context", "response") ), which should natively, from our code, trigger the
     evict_if_needed method, given we hardcoded the frequency to one.
    Lastly, we do the assertion checks and verify, within the evict_if_needed method, if:
     ○ zpopmin method was actually called once;
     ○ the delete method actually deleted the mocked document, the one with ID "doc_to_evict".
 • test_semantic_cache_stats - we check the correct functionality of the stats method, which originally checks how many
    Chroma entries there are in the collection and if there is consistency between the number of Chroma entries in the
    collection and the number of LRU istances present in the Redfis system (we recall LRU is NOT native in Chroma). We
    note that, the good pairing between Chroma entry & LRU detials has been checked with the tests done with
    the add method. We do it for the case where there is consistency.
    As always, first, we mock the Chroma collection, but we do not need to mock the embedding operations this time, as
     we are not adding any new entry, therefore, we do not need any embedding conversion, just the zcard method.
    We mock the Redis and Chroma count to be both 69, which should means that the stats method should return
     consistency between Redis and Chroma.
    We do the usual patches for the Redis server and the SentenceTransformer (see Remark below) istances.
    Last, we call the stats method and proceed with the assert checks:
     ○ if the Chroma collection number of entries is 69, as we chose;
     ○ if the Redis number of entries is 69, as we chose;
     ○ if the program sees the pairing as consistent (as it should).
 • test_semantic_cache_stats_inconsistency - we check the correct functionality of the stats method and we do it for the
    case where there is not consistency.
    Exactly same layout as before, but we set different values for the Redis and Chroma count, namely 67 and 69.
    We check, by assert method, if the program return the cosistency check as failed, hence if consistent is False.
 • test_semantic_cache_close - we test the closure of both the Redis and Chroma connections.
    Usual setup, but we mock the close client method (we close the Redis with - await cache.close() -, which invokes
     the real closing method). This means we are closing the Redis and Chroma "mock-multiverse" connections
    We, finally, assert that the closing method has been closed onceboth for the Redis and for the Chroma clients.
 REMARK "WHY WE ALWAYS PATCH THE SENTENCETRANSFORMER EVEN THOUGH WE DO NOT ACTUALLY CREATE EMBEDDINGS?" We always have
  to patch it (and so call it) because SemanticCache.__init__ always runs the line
  - self.embedder = SentenceTransformer(settings.embedding_model_name) - upon execution, always of the __init__, hence
  it always loads the embedding model by default, even if we do not explicitly call it. If we do not mock it, the real
  SentenceTransformer will actually try to load a model, exiting out of the testing scope we are working to show,
  redoundant and useless for unit testing.
REMARK: @pytest.mark.asyncio: this marker ensures that the tests that call async methods are async themselves, so that
 pytest does run them correctly; without it, pytest would treat it as a regular function, which gets called and checks
 the return value. The tests would pass silently, but the proper assert logic to actually check the tests would not be
 executed. Long story short, it tells pytest that, what we created, is a asynchronous coroutine (the ones defined with
 async def) and should be executed inside an asynchronous event loop, substitues writing try: and finally: every time.
 We remind that, in simple words, a coroutine is an object that represent a function in python that can be stopped and
 resumed.

------------------------------------------------------------------------------------------------------------------------

TEST_TOKEN_CONTROL.PY
This script test the TokenValidator class from token_control-py, which counts tokens used by the HuggingFace tokenizer
 and has three configurable strategies (which we test): reject, truncate_with_warning and summarize_overflow and,
 lastly, reroutes unknown strategies to the default, reject.
The code works as follows:
 • mock_tokenizer - We define the reusable fixture @pytest.fixture and create a mock tokenizer, through MagicMock, which
    replaces the HuggingFace one, the encode returns a list made of numbers up until the count of tokens, namely equal
    to the number of characters (just as an example) and the decode returns a dummy string, though we do not use it in
    the test.
   We create tow lambda functions:
    1. the first takes a string and returns a list of integers from 0 up to the length of the text - 1, as a very simple
        encoding simulation;
    2. the second takes a list of integers (tokens IDs), we call "tokens" and returns a string that we defined as
          ''.join(chr(ord('a') + t % 26) for t in tokens)
        This is made of:
         ○ ''.join(...) - which merges the generated characters into a single string;
         ○ chr() converts ASCII code into a single character;
         ○ we take every single token ID, which we call "t" and we return the ASCII code of a lowercase letter, made of
            ord('a')=97, the ASCII code of the letter a (an integer, in general) plus each letter's token ID and,
            finally, by writing % 26, we ensure the result stays between 0 and 25 (namely between letter "a" and "z").
            For example [0,1,2] returns abc.
    REMARK ON LAMBDA: lambda acts like a "quick-use" function, where we define:
           variable_result = lambda argument_of_the_function : result_of_the_function
     where a comma separates the list of inputs (arguments) and a SINGLE expression (result) is returned.
    REMARK ON SIDE_EFFECT: if we used .return_value, the result of the function would have been the same, regardless of
      the input; in our case, we use .side_effect as it can return different values depending on inputs, exception raised
      or side effects like logging. Using return_value, would return the same token list, regardless of the prompt,
      breaking our tests.
     For example:
      mock = MagicMock()
      mock.return_value = 42
      print(mock("anything", arg2=123))  #returns 42
      print(mock("different"))           #returns 42
     Unlike:
      mock = MagicMock()
      mock.side_effect = lambda x: 2 * x
      print(mock(5))    #returns 10
      print(mock(10))   #returns 20
 • test_prepare_prompt_under_limit - it verifies that, if the prompt is under the max_prompt_tokens, it returns it
    unchanged, with truncated = False and showing the "correct" (the mocked one, in our case) token count.
   First we patch the former tokenizer with the mock one we defined before and, with the patch, we rename the former
    class TokenValidator as validator.
   We create a short mock prompt with the string "short".
   Before proceeding, we recall that, the original prepare_prompt method returns a tuple made of a string (our prompt)
    and a dictionary of one element made of True/False if there is truncation and the original token count. In our mock
    case, we run the prepare_prompt method and return the string "processed" and the "meta"(data) dictionary.
   We finish with the usual assertions, check if:
    ○ the string returned by prepare prompt is the correct one, in our mock example "short", namely the prompt;
    ○ the first element of the dictionary, relative to the True/False for the truncation, which should return False,
       as the string we mocked is less than the max_prompt_tokens we patched as 100;
    ○ the second element of the dictionary corresponds to the correct token count, relative to the "handmade" tokenizer,
       which, we recall, counts the characters, which should return 5 for the prompt "short".
 • test_prepare_prompt_reject_strategy - we test the correct functioning for the first (and default) strategy reject.
   As before, we set up (patch) the mocked tokenizer and rename the the TokenValidator class, to use its methods and
    test them.
   We patch the max_prompt_tokens to be 10, low enough, and the overflow_strategy to reject, reasonably for our test.
   We create a dummy prompt longer than 10 words, to check if the strategy is actually triggered by prepare_prompt.
   We run the prepare_prompt method and catch, from pytest, the TokenLimitExceededError error returned by the original
    code and rename it, in order to proceed with the assertions.
   Finally, we do the assertions:
    ○ we assert that the TokenLimitExceededError error is raised inside pytest;
    ○ we check if the exc(eption) is actually because the token_count is over the max_prompt_tokens = 10;
    ○ we check the value for the max tokens stored into the exc(eption) to actually be 10, as we patched up in the mock
       environment.
 • test_prepare_prompt_truncate_with_warning - we test the correct functioning for the truncation strategy.
   As before, we set up (patch) the mocked tokenizer and rename the the TokenValidator class, to use its methods and
    test them.
   We patch the max_prompt_tokens to be 10, low enough, and the overflow_strategy to truncate_with_warning, reasonably
    for our test, and the "truncation_keep_start_ratio" to 0.6.
   We create a dummy prompt longer than 10 words, to check if the strategy is actually triggered by prepare_prompt and
    we run the prepare_prompt method, returning the string processed and the dictionary meta(data).
   Finally, we do the assertions, checking the truncated_prompt return of the _truncate_sliding_window:
    ○ we check if the truncated method is triggered, hence True;
    ○ we check if the strategy called is truncate_with_warning;
    ○ we check if "warning" is present in the meta(data) dictionary;
    ○ we check if "original_token_count" is present in the meta(data) dictionary;
    ○ we check if "truncated_token_count" is present in the meta(data) dictionary;
    ○ we check if the token length token_len of our truncated prompt is actually within the limits, hence less or equal
       than 10.
 • test_prepare_prompt_summarize_overflow - we test the correct functioning for the summarization strategy.
   As before, we set up (patch) the mocked tokenizer and rename the the TokenValidator class, to use its methods and
    test them.
   We patch the max_prompt_tokens to be 10, low enough, and the overflow_strategy to summarize_overflow, reasonably
    for our test, and the "summarization_keep_ratio", namely the percentage to keep as it is,
    without summarizing to 0.6.
    Moreover, we force the mock summarizer to return the string "Fake summary", via MagicMock().
   We create a dummy prompt longer than 10 words, to check if the strategy is actually triggered by prepare_prompt and
    we run the prepare_prompt method, returning the string processed and the dictionary meta(data).
   Finally, we do the assertions, checking the truncated_prompt return of the _summarize_overflow:
    ○ we check if the truncated method is triggered, hence True;
    ○ we check if the strategy called is summarize_overflow;
    ○ we check if "warning" is present in the meta(data) dictionary;
    ○ we check that the summarized version returns "Fake summary", which is the summarizations we "force" through our
       mock summarizer;
    The truncation assert, per-se, have been checked in the test_prepare_prompt_truncate_with_warning set of tests.
 • test_prepare_prompt_unknown_strategy_fallback - verifies that, upon selecting a strategy that is not one of the three
    we defined, it rollbacks to the default reject strategy. To do so, to actually raise the TokenLimitExceededError
    error, we use a long enough prompt that exceeds the usual patched max_prompt_tokens.
   We do as before, but set the strategy to be invalid, in particular to be "unknown_strategy" and we set the
    max_prompt_tokens to 10.
   Finally, we do the assertions, checking if:
    ○ the TokenLimitExceededError error has been raised by pytest, hence if we actually rolled back to the reject
       strategy.

------------------------------------------------------------------------------------------------------------------------

TEST_LLM_CLIENT.PY
This script tests the functionality of the llm_client.py script, which initializes the ChatGroq LLM instance, together
 with the call_llm_with_reply method, which retries the LLM call upon specific error instances, made of httpx
 exceptions, in particular the NetworkError, TimeoutException and HTTPStatusError errors, and, finally, returns an
 exception, hence an error, when the max number of retries have been tried. We will check that no retries are triggered
 upon other exceptions, as by design of our original process as well as we check that the algorithm actually stops after
 reaching the max number of retries, originally set up in config.py.
These tests will check the correct functioning of the tools we used in llm_client.py, from the tenacity library,
 for the retries setup.
The code is structured as follows:
 • test_llm_client_generate_success - verifies that, upon a succeeded LLm call, the generate method returns the expected
    response without going into the retry finite loop.
   We mock the llm with AsyncMock to start and force its response, upon using the .invoke method) to be the
    "Mocked response" (simulating a successful LLM response) string and patch the llm client to be our mock_llm.
   We create the client, using the mock LLM.
   Lastly, we call the original generate method and begin the assertions:
    ○ we check if the response is fetched correctly from our mock LLM, namely if it is "Mocked response";
    ○ we verify if the ainvoke method was actually called once, namely we check that no retries have been done
 • test_llm_client_retry_on_network_error - we verify that, upon receiving a NetworkError, the retry decorator kicks in
    and triggers a retry for the LLM call, for simplicity, we assume the second call to work, with our mock and patches.
   We patch the original groq LLM and replace it with our mock one and we mock the ainvoke method to return, on first
    instance a NetworkError with description "Fake network error" and, second, we mock a successful call.
   As before, we call the original LLMClient class and run the generate method on a mocked test prompt.
   We, last, check with assert that:
    ○ the actual final result is NOT the first one of our mock ainvoke, but the second one, namely, the original
       generate actually saw the NetworkError;
    ○ to make sure, we check that the ainvoke method was called exactly twice, hance the retry decorator actually ran.
 • test_llm_client_retry_on_timeout - exactly as the test before, but for the error TimeoutException; same asserts too.
 • (the test with HTTPStatusError would be exactly the same)
 • test_llm_client_no_retry_on_other_exceptions - in this case, we test that, upon an error not in the
    retry_if_exception_type list, no retries are triggered, but, still, the error text is shown tp the user.
   We check if
    ○ the error raised up is defined by the text we chose in the mock;
    ○ the ainvoke method was actually called one, hence no extra call are made upon unknown errors.
 • test_llm_client_max_retries_exhausted - we check that, in our case, setting up the mock LLM to return the same
    NetworkError three times, after the third, it actually stops looping. We recall that, loading from the original
    class, if we do not patch the retry_attempts, they will be loaded normally from config.py, hence, in this case,
    they are, as normal default, at most 3.
   Similar code composition, we check that
    ○ the correct error is raised all the three times;
    ○ the ainvoke method is actual ran exactly three times

------------------------------------------------------------------------------------------------------------------------

TEST_MAIN.PY
This script tests the FastAPI endpoints defined in main.py, namely /chat, which handles cache hits/misses, token limit
 rejections and Network errors, /cache/stats, which returns stats about the cached response stored and /health, which we
 set up to return by default {"status": "ok"}, but can be generalized and expanded as a concept,
 just a sanity check for us.
The script works as follows:
 • client() - we write this fixture to override the default configurations of our "real" main.py, replacing them with
    mocks; the yield will return the TestClient instance to the test functions, with the client parameter, WITHOUT
    exiting the with block, which are still active, because the yield is inside them. Therefore, out test function runs
    using client to make requests to the pathched app.
    We do NOT use return TestClient(app), we would exist the function, hence we would exit the with blocks, which we
    WANT to use during the tests; this way, we do the tests WITH the with blocks and, when the test is finished, we go
    back to the "normal" client
 REMARK ON YIELD: the yield is a keyword that takes a function and returns a generator, which is an iterator which
  pauses at execution, returns a value to the object that called it and, after that, proceeds from where it paused.
  Unlike return, which terminates the execution (take N objects and return and object result), the generator pauses the
  function, generally for checking intermediate values.
  Below is a very simple example that spicifies the idea:
      '''
      def my_generator():
          print("Start")
          yield 1
          print("Middle")
          yield 2
          print("End")
      gen = my_generator()
      next(gen)  # Prints "Start", returns 1, pauses
      next(gen)  # Prints "Middle", returns 2, pauses
      next(gen)  # Prints "End", raises StopIteration
      '''
 • test_health_endpoint - simple check that the /health() endpoint actually returns "200 OK" with {"status": "ok"}, as
    we decided in the original project.
 • test_cache_stats_endpoint - tests that the /cache/stats endpoint actually returns the correct statistics about the
    sizes of the exact and semantic cache store, as well as a correct status_code upon calling the GET method and the
    max_entries for the sematic cache, which would originally be taken from max_semantic_cache_entries in config.py, in
    our case we patch it before testing.
   After using AsyncMock to mock the exact and semantic cache entries, we check via assert that, upon calling
    .get("/cache/stats"), we get that cache stats returned correctly, by checking the json given by the response, namely
    we assert that:
    ○ the response to the .get("/cache/stats") is regular, namely statsu_code = 200;
    ○ the exact_cache_size and semantic_cache_size is the one set through the mock;
    ○ the semantic_cache_max_entries is the one set through the mock.
    We recall that the json returned by using the .get("/cache/stats") method has the following fields:
     exact_cache_size, semantic_cache_size, semantic_cache_max_entries, cache_hit_rate_total, cache_hit_rate_exact,
     cache_hit_rate_semantic, total_requests, total_tokens_saved.
 • test_chat_endpoint_exact_cache_hit - we verify that, upon a exact cache hit, the response is given directly, without
    calling the LLM.
    We, first, mock the get method for the exact_cache to hit and return the string "exact answer" and for the
     semantic_cache to miss.
    Then, we send a request through /chat, through a POST request with a dummy json body (correct metadata for the
     original POST method to work and we start with the checks:
     ○ we check that we actually returned a response (status_code = 200)
     We, then, save the response, which should be the one recycled from the mock exact_cache and check if:
     ○ we got a cache_hit (hence if it is True in the response meta(data);
     ○ the cache_hit is of exact time (and not semantic), as we set up through the mocks;
     ○ the answer is the one set up in the mock exact_cache.
 • test_chat_endpoint_semantic_cache_hit - very similar as the exact method, but, this time, we trigger the mock
    semantic_cache to get a hit, setting the similarity to 0.95 (we recall that, to correctly get a semantic_cache hit,
    the similarity MUST be above the similarity_threshold, which we did not patch, hence will be the original one, set
    to default to be 0.92 in config.py) and the exact_cache to miss.
   We proceed with the same asserts as per the exact_cache tests.
 • test_chat_endpoint_cache_miss - we test the correct functionality in the case where both cache, exact and semantic,
    miss, to properly trigger the LLm response and check if it is registered correctly in the response JSON.
   We mock both exact and semantic cache to return None upon calling the get methods.
   We mock the token validator prepare_prompt method to use "processed prompt" as prompt to send the LLM and to not
    reject nor truncate, setting the mock token_count for the processed_prompt to 50 (below the original
    max_prompt_tokens = 7000).
   Then, we mock generate a response from the LLM to be "LLM generated answer" and mock the count token for
    the response to return 100.
   Later, we use the .post method to actually receive the (mocked) LLM reponse and store it with its metadata.
   Last, we move one to the usual assertions:
    ○ we assert the response to have been processed correctly (status_code = 200);
    ○ we check that there was not cache hit (hence no cache type);
    ○ we check the generated answer to actually be the LLM-mocked one we setup previously;
    ○ we check the amount of tokens sent and received to be the one we chose with the mock;
    ○ we get the set/add method for the exact/semantic_cache to be ran once, given we generate a new answer
       (we do not care about the storing, checks for the exact and semantic cache have been thoroughly done before).
 • test_chat_endpoint_token_rejection - we test the /chat endpoint to actually return a structured error response upon
    exceeding the max_prompt_tokens, by default set, even in config.py, to 7000. We recall that the overflow_strategy by
    default is "reject", hence should return HTTP (status_code) 400 with error  TokenLimitExceededError.
   We mock the token_validator to return, upon calling the .prepare_prompt method, a TokenLimitExceededError for using
    8500 tokens, instead of 7000 max_prompt_tokens.
   We simulate posting a prompt with a very song text ("old man talking" * 1000).
   After checking that the .post method returns us an error with status_code 400 (HTTP 400), we create the JSON response
    and check (assert) if its meta(data) is correct, namely we check if:
     ○ the error raised is "TOKEN_LIMIT_EXCEEDED", as decided in main.py;
     ○ the token counts for the prompt and the generated response are the correct (mocked) amounts.
 • test_chat_endpoint_llm_service_unavailable - we verify if the program actually reports the correct error when the LLM
    client raises an exception, namely after all retry_attempts have been exhausted.
    It works as follows:
     ○ we mock the exact and semantic caches instances and the prepare_prompt method to return empty (not needed), as
        well as the generate method for the llm_client to return the exception "LLM connection error";
     ○ through the /chat endpoint (POST method), we generate a response and check if it returned status_code error 503,
        which should return "LLM_SERVICE_UNAVAILABLE";
     ○ upon generating the JSON (meta)data of the response, we check if the error is actually "LLM_SERVICE_UNAVAILABLE";
        and if the error message (in the metadata) is the one we set up with the mock.

========================================================================================================================
========================================================================================================================

GENERAL NOTES AND HEADS-UP FOR LEARNING
1. UNDERSCORE BEFORE METHODS/FUNCTIONS IN PYTHON.
    In Python a _ (underscore) before a method means that given method is for INTERNAL use only or protected in some
     way: it signals to other (maybe future) developers and other tools that this method is NOT part of the public API
     and should NOT be called outside the class.
2. SELF (We use self.XXXX many times in our project, it is worth to write a detailed explanation of its features).
    self method is a crucial technique in OOP (Object Oriented Programming) in Python. self is a conventional parameter
     name used to pass a method on an instance, using the instance itself as the first argument and it refers to the
     specific instance of the class on which the method is called. Basically it serves to tell which instance is the
     method operating on.
    Let us explain some snippets of our code as an example and an external, simple one:
     • class ExactCache:
          def __init__(self):
              self.redis = redis.from_url(...)   # self.redis belongs to the instance
      This created two instances, cache1 and cache2. Without self, the method would have no way to distinguish between
       the two instances
     • class Mynumber:
           def __init__(self, value):
               self.value = value
           def print_value(self):
               print(self.value)
       obj1 = Mynumber(17) - creates an object named obj1. In __init__(), self will refer to obj1.
       self.value = value - stores 17 in the object's value attribute.
       obj1.print_value(17) - prints the value, namely 17.
     When - obj1.print_value() - is called, self, again, refers to obj1, therefore its attribute (the value 17) is
      printed.
3. MODEL MATCHING BETWEEN TOKENIZER AND SUMMARIZATION
   Different models use different tokenization techniques, algorithms and vocabularies (like special character
    interpretation), namely the process of converting words and strings into a sequence of integers IDs (aka tokens),
    understandable by the LLM used. For the same string, model tokenize different, producing different token
    sequences, hence resulting in different token amount computations (even if small). To give some examples,
    GPT-4 uses cl100k_base tokenizer, Llama 3 uses SentencePiece tokenizer (based on Hugging Face),
    Mistral uses SentencePiece too, but with a different vocabulary size. Using the same token counter for all three
    might result imprecise, even for the same sentence, leading to unexpected and incorrect max_prompt_tokens errors.
   We use
        self.tokenizer = AutoTokenizer.from_pretrained(settings.tokenizer_model_name)
    which loads the exact tokenizer relative to the LLM we use as main model. A possible scalable ideal option would be
    to provide a list of tokenizer relative to the main model, though it is more consistent to do it "by hand" once the
    LLM is selected; in this project we use Groq, which is compatible with the Llama 3 tokenizer family.
   In our project, the tokenizer is used for
    1. counting tokens in the prompt (query + context);
    2. truncating the prompt (in case we use the sliding window method);
    3. counting the tokens and tokenizing the prompt before applying the summarization method (if is the one used)
        and redecoding the summary produced into strings (aka text).
4. TESTABILITY
    To ensure the project works flawlessly, we create a mock control setup, testing all the scenarios studied, in depth;
     to achieve this, a new section of files, grouped in a subfolder called tests, using the library pytest.
    In our project, we use ainvoke as an asynchronous method that returns us the result of an asynchronous function
     (coroutine), used in frameworks like LangChain and LangGraph; to mock this method, we will use AsyncMock,
     which will simulate the async function, returning an awaitable result when called.
    These methods allow us to be absolutely certain that the raw project works, not considering "natural" problems
     given by the actual API model exceptions which arise naturally eventually.
5. DECORATORS
In our project, we often used decorators, mostly in the Mock part of the project to slightly modify the original
 functions.
A decorator is a common design in Python that lets the developer to modify or generalize the behavior of an existing
 function, method or class, WITHOUT changing the original logic behind the source code or its functioning. It can be
 compared to adding a laser to a gun, the original gun still works, but the laser gives it an extra functionality,
 without damaging or internally changing the original gun.
Below we make a very simple example:
'''
def logger(func):
    def wrapper():
        print(f"Starting: {func.__name__}")
        func()
        print(f"Finished: {func.__name__}")
    return wrapper

@logger
def connect_to_database():
    print("Connecting...")
    print("Connected!")

connect_to_database()
# Output:
# Starting: connect_to_database
# Connecting...
# Connected!
# Finished: connect_to_database
'''




















